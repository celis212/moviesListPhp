<?php
/**
 * performs the connection and requirement with the bootstrap.php file
 * the core instance is Init 
 */
    //call the bootstrap ans start the core class
    require_once '../app/bootstrap.php';

    $start = new Core;